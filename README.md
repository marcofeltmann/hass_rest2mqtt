# REST2MQTT

This Home Assistant integration should call multiple REST services, store
selected JSON data into an MQTT server, and provide a sensor for each of the
data selections to use in Home Assistant.

## Current State

In planning
